#include <stdio.h>
#include <math.h>
int main()
{
    float a, b;
    printf("Введите длину отрезка А: ");
    scanf("%f", &a);
    printf("Введите длину отрезка B (B < A): ");
    scanf("%f", &b);
    if (a>b)
    {
        float c=floor(a/b);
        printf("На отрезке А можно разместить %g отрезков В.", c);
    }
    else
    {
        printf("Введены неверные значения!");
    }
}

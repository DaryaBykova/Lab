#include <stdio.h>
#include <locale.h>
int main()
{
    setlocale(LC_ALL, "Russian");
    setlocale(LC_CTYPE, ".1251");
    int n;
    char a[5] = "NOZW";
    char c;
    printf("Введите символ С, обозначающий исходное направление робота(«N» — север, «W» — запад, «N» — юг, «O» — восток)");
    scanf("%c", &c);
    printf("Введите команду(0 - продолжать движение, 1 - поворот налево, -1 - поворот направо):");
    scanf("%d", &n);
    if (c == a[0])
    {
        if (n == -1)
        {
            printf("Направление робота после выполнения команды: O");
        }
        else if (n == 0)
        {
            printf("Направление робота после выполнения команды: N");
        }
        else if (n == 1)
        {
            printf("Направление робота после выполнения команды: W");
        }
    }
    else if (c == a[1])
    {
        if (n == -1)
        {
            printf("Направление робота после выполнения команды: Z");
        }
        else if (n == 0)
        {
            printf("Направление робота после выполнения команды: O");
        }
        else if (n == 1)
        {
            printf("Направление робота после выполнения команды: N");
        }
    }
    else if (c == a[2])
    {
        if (n == -1)
        {
            printf("Направление робота после выполнения команды: W");
        }
        else if (n == 0)
        {
            printf("Направление робота после выполнения команды: Z");
        }
        else if (n == 1)
        {
            printf("Направление робота после выполнения команды: O");
        }
    }
    else if (c == a[3])
    {
        if (n == -1)
        {
            printf("Направление робота после выполнения команды: N");
        }
        else if (n == 0)
        {
            printf("Направление робота после выполнения команды: W");
        }
        else if (n == 1)
        {
            printf("Направление робота после выполнения команды: S");
        }
    }
}

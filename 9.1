#include <stdio.h>
#include <math.h>
int main()
{
    int n;
    printf("Введите кол-во (N) секунд, прошедших с начала суток (N - целое): ");
    scanf("%d", &n);
    int n1=n - floor(n/60)*60;
    printf("Кол-во секунд, прошедших с начала последней минуты: %d", n1);
}

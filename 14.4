#include <stdio.h>
int main ()
{
    float p, s=1000, k;
    printf("Введите кол-во процентов Р(0<P<25): ");
    scanf("%f", &p);
    p=p/100;
    while (s<=1100)
    {
        k++;
        s=s+s*p;
    }
    printf("Вклад превысит 1100 р. через %g мес. и составит %g р.", k, s);
}

#include <stdio.h>
int main()
{
    int a, a1=1, q=0;
    printf("Введите целое положительное число: ");
    scanf("%d", &a);
    for (a1=1; a1<=(2*a - 1); a1=a1 + 2)
    {
      q=q+a1;
    }
    printf("Квадрат введенного числа: %d", q);
}

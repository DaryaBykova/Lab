#include <stdio.h>
#define _CRT_SECURE_NO_WARNINGS_
int main()
{
    float x;
    printf("Введите значение переменной x=");
    scanf("%f", &x);
    float x2=(x-3)*(x-3)*(x-3);
    float y=4*x2*x2-7*x2+2;
    printf("Значение функции у=4(x-3)^6 − 7(x-3)^3 + 2 при заданном значении x=%f", y);
}

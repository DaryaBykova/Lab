#include <stdio.h>
int main()
{
    int a, b, i, i1;
    printf("Введите два целых положительных числа - А и В(А<B): ");
    scanf("%d", &a);
    scanf("%d", &b);
    for (i=a; i<=b; i++)
    {
        for (i1=1; i1<=i; i1++)
        {
            printf("%d", i);
        }
    }
}
